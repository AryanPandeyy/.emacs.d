# -*- mode: snippet -*-
# name: create-next-page
# uuid:
# key: nfc
# condition: t
# --
import { FC } from 'react';

interface `(file-name-sans-extension (file-name-nondirectory (buffer-file-name)))`Props {
  $1
}

const `(file-name-sans-extension (file-name-nondirectory (buffer-file-name)))`: FC<`(file-name-sans-extension (file-name-nondirectory (buffer-file-name)))`Props> = () => {
  return <div>`(file-name-sans-extension (file-name-nondirectory (buffer-file-name)))`</div>
}

export default `(file-name-sans-extension (file-name-nondirectory (buffer-file-name)))`
